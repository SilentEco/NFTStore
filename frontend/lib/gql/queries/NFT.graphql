query GetOneNFTs($id: ID!) {
  nftCards(filters: { id: { eq: $id } }) {
    data {
      id
      attributes {
        Title
        NFT {
          data {
            attributes {
              url
            }
          }
        }
        Artist
        Created
        Price
      }
    }
  }
}
